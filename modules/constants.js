const ACCESS_TOKEN_HEADER = 'x-token';
const SECRET_KEY = 'secretkey';

const LOGIN_EXIST = 'LOGIN_EXIST';
const EMAIL_EXIST = 'EMAIL_EXIST';

module.exports = {
  ACCESS_TOKEN_HEADER,
  SECRET_KEY,
  LOGIN_EXIST,
  EMAIL_EXIST
};
